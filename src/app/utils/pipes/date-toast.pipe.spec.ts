import { DateToastPipe } from './date-toast.pipe';

describe('DateToastPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToastPipe();
    expect(pipe).toBeTruthy();
  });
});
