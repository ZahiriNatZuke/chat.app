import { ReferenceLimiterPipe } from './reference-limiter.pipe';

describe('ReferenceLimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenceLimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
